# derekbarnhart.github.io

Local development

- Serve static files with Python's built-in server:
  - `make serve` (defaults to `http://127.0.0.1:8000`)
  - Override host/port: `HOST=0.0.0.0 PORT=3000 make serve`
